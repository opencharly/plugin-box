# 2026.266.1110 — feat(box): migrate the load venue to the data-engine jump (engine cutover)

## Summary

Consumer leg of the **engine provider class** cutover (spec#144/#145, sdk#272 — both merged). The spec removed the per-engine jump arms and made the engine **DATA** on the jump (`NestedJump.Engine`); `charly box load`'s probe hop is the consumer.

- the probe jump is now the ONE `JumpContainerExec` arm with `Engine :=` the engine `ResolveContainer` returned, so the probe hop and the load command address the venue through the **same resolved binary** (the property the former comment claimed, now structurally true)
- the `strings` import dies with the `strings.Contains(engine, "docker")` special-case it served
- spec pin `v0.2026251.1735` → `v0.2026266.545`; sdk `v0.2026250.527` → `v0.2026266.623`. The `go.sum` also completes the standalone (workspace-masked) transitive entries — `go-isatty` go.mod hash + the relocated `plugin-egress`/`plugin-init` require lines.
- the removed-identifier comment is swept (R5), so the deleted-identifier grep is empty.

## How tested

**Change-class gate:** `charly check run check-boxload-pod` (kind:pod, `disposable: true`), driven with the branch-built engine-cutover charly `2026.266.0826` and the `plugin-deploy-pod` wave leg composed via the documented `CHARLY_REPO_OVERRIDE` verify-before-push mechanism.

**The changed path executes live** — the bed's own plan steps are exactly the `charly box load` path this PR changes:
```
  PASS  check box load delivers the host image into the pod's nested uid-1000 store  [boxload-deliver]  exit=0
  PASS  check the delivered image is present in the NESTED store, queried through the socket  [boxload-present-in-nested-store]  exit=0
```

**Fresh rebuild:** the bed's step 6 is `charly update` (`[update] PASS 2m13.748s`), with `[check-live-rebuild] PASS 1m38.688s` re-evaluating against the rebuilt container.
```
$ charly check run check-boxload-pod
[deploy-add] PASS 40.88s  [config] PASS 32.443s  [start] PASS 25.826s
[check-live] PASS 1m43.056s  [feature-run] PASS 47.437s
[update] PASS 2m13.748s  [check-live-rebuild] PASS 1m38.688s
[feature-run-rebuild] PASS 46.396s  [cleanup] PASS 29.628s
PASS (steps=14)
```
`.check/check-boxload-pod/2026.266.0941/summary.yml` → `ok: true`, every step `ok: true`, 0 failed steps.

Local gates (standalone, `GOWORK=off`): `gofmt -l .` (empty) · `go build ./...` · `go vet ./...` · `go test ./...` GREEN.

**R5 sweep (executed):**
```
$ grep -rn "JumpPodmanExec\|JumpDockerExec" --include='*.go' candy/plugin-box
(no matches)
```

## Project-rulebook rule-compliance

- **R0** — `/charly-check:check`, `/charly-internals:plugin`, `/charly-internals:git-workflow` loaded first.
- **R1** — the transient build failure surfaced during the leg was root-caused (incomplete standalone `go.sum`, workspace-masked) and fixed in-tree.
- **R2** — the sibling wave legs (schema 249→261) are named on opencharly/charly#633, not parked.
- **R3** — one `JumpContainerExec` arm, engine as data; no second per-engine path.
- **R4** — no shim; the removed arms are gone.
- **R5** — the deleted identifiers are swept (comment included); the grep above is empty.
- **R6/R7** — green build/vet/test + the real disposable bed above.
- **R8** — no emitted artifact changed (plugin Go only).
- **R9** — the bed runs the in-development toolchain (branch binary `2026.266.0826`), not a stale release.
- **R10** — the `check-boxload-pod` fresh-rebuild cycle above, on a `disposable: true` target.
- **RDD** — the engine→jump pairing was proven live in the bed.
- **ADE** — the candy's baked plan is unchanged and ran.
- **Disposable-only autonomy** — the only destroy is the bed's own `disposable: true` teardown.

## Change classification

Plugin Go code on a runtime path (`charly box load` probe-jump wiring) + dependency pin bump. Verification gate: the `check-boxload-pod` disposable R10 bed (fresh rebuild) + standalone Go gates. Attribution tier: `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*