# 2026.251.1834 — refactor(plugin-box): F4.1/F4.2/F4.3 — cross-cutting dedup (Cutover F)

## Summary

Cutover F plugin-box leg — three cross-cutting deduplications in one PR.

**F4.1 — validatePlanStepsPure (byte-identical to kit.ValidatePlanSteps).**

Duplication evidence: `validate_rules.go:789-791` defines `validatePlanStepsPure` — the exact body of `kit.ValidatePlanSteps` (sdk/kit/planvalidate.go) — while plugin-box already imports kit at `validate_rules.go:21`.

Fix: delete the copy; `validate_rules.go:92` calls `kit.ValidatePlanSteps`.

**F4.2 — provider-class vocabulary held twice → ONE source in spec.**

Duplication evidence: `validate_rules.go:150-170` — `validPluginClasses` 11-entry closed map, comment admits "mirrors core providerClasses"; the byte-identical copy in charly/provider.go:78-95 (deleted in charly#571); the `#PluginCapability` regex in spec/schema/candy.cue carried the same 11 classes (reworked in spec#125).

Fix: `validPluginClasses` is derived from `spec.ProviderClasses` — the ONE CUE-owned list (spec#125: `#ProviderClassNames` → schemagen → `spec.ProviderClasses`). New `TestValidPluginClassesParity` locks the bijection.

**F4.3 — remote-ref parser ×2 → the canonical spec.ParseRemoteRef.**

Duplication evidence: `validate_rules.go:896-949` — `parsedRef` + `parseRemoteRef` + `splitRepoAndSubPath` mirroring the pure charly refs.go parser, with a STALE pointer to the deleted `refs.go` in its comment. The canonical `spec.ParseRemoteRef` (spec/spec/ref_parse.go) is byte-identical.

Fix: the mirror is deleted; the two "remote candy not found" diagnostic sites (`:45`, `:540`) call `spec.ParseRemoteRef`. The stale refs.go comment dies with it.

Spec pin bumped to v0.2026251.1735.

## How tested

```
$ cd plugin-box/candy/plugin-box
$ go build ./... && go vet ./... && gofmt -l .
(empty, exit 0)
$ go test -run TestValidPluginClassesParity . -v
=== RUN   TestValidPluginClassesParity
--- PASS: TestValidPluginClassesParity (0.00s)
$ go test ./...
(no failures)
```

## R10 — fresh disposable-bed run (branch-built plugin-box)

Ran `charly check run check-commands-local` — the plan-roster plugin-composition + box-verb bed (disposable kind:local on host: local; ONE of its live checks drives `charly box new` + **`charly box validate`** + `charly box generate`, so the box validate engine this PR rewrites — `validate_rules.go` — executes live) — with the **branch-built plugin-box** compiled into the charly binary via the E-2 recipe: a scratch charly clone at origin/main `4bb44a76` (post-F1/F2/F4.2 wave pins) with a `go work` replace of `github.com/opencharly/plugin-box/candy/plugin-box` to this branch's final committed tree (`1da65db`, the PR head). The bed project is the charly clone — NOT the plugin-box repo tree.

```
$ cd /tmp/ch-f4pb-scratch            # scratch charly clone @ origin/main 4bb44a76
$ go work edit -replace github.com/opencharly/plugin-box/candy/plugin-box=/home/atrawog/Sync/Atrapub/coder/pi/opencharly/plugin-box/candy/plugin-box
$ go list -m github.com/opencharly/plugin-box/candy/plugin-box
github.com/opencharly/plugin-box/candy/plugin-box v0.2026250.1030 => /home/atrawog/Sync/Atrapub/coder/pi/opencharly/plugin-box/candy/plugin-box
$ CALVER=$(bash scripts/calver.sh)   # 2026.251.1819
$ cd charly && go build -buildvcs=false -ldflags "-X main.BuildCalVer=${CALVER}" -o ../bin/.charly.next .   # exit 0
$ cd .. && mv bin/.charly.next bin/charly && ./bin/charly version
2026.251.1819
$ ./bin/charly check run check-commands-local
charly check run check-commands-local: [deploy-add] PASS after 1m3.455s [allowlisted=1] (log: .check/check-commands-local/2026.251.1828/deploy-add.log)
charly check run check-commands-local: [bring-up-members] PASS after 0s
charly check run check-commands-local: [check-live] PASS after 36.54s [allowlisted=1] (log: .check/check-commands-local/2026.251.1828/check-live.log)
charly check run check-commands-local: [update] PASS after 10.038s [allowlisted=1] (log: .check/check-commands-local/2026.251.1828/update.log)
charly check run check-commands-local: [cleanup] PASS after 5.926s [allowlisted=1] (log: .check/check-commands-local/2026.251.1828/cleanup.log)
charly check run check-commands-local: [cleanup-members] PASS after 0s
charly check run check-commands-local: PASS (steps=6)
```

Calver `2026.251.1828`; summary.yml `ok: true`, errors 0, warnings 0 (only the informational dev-worktree-binary-notice, allowlisted by the bed itself). Artifacts: `.check/check-commands-local/2026.251.1828/` (per-step logs + summary.yml).

The changed validation path executed live: the check-live matrix (8/8 PASS) includes `[box-new-validate-generate]` — "validate via the plugin's own validate ENGINE over the HostBuild(\"validate-project\") resolved-project envelope" — i.e. `charly box validate` on the scaffolded scratch project runs the F4.1/F4.2/F4.3 `validate_rules.go` compiled from this branch. Base→head diff proof (`git diff d01ad77 1da65db`): `validate_rules.go | 94 ++++++++------------------------------ (18 insertions, 76 deletions)` — the compiled path provably differs from the pre-merge base.

Released-pin re-check: the same bed (and the one-pod/one-VM roster) re-runs on the consolidated binary after this PR's tag — the scheduled F-wave close R10, not a deferred substitute.

## Rulebook compliance

- **R0 (skills honored):** R0 dispatcher skills (go, git-workflow, strict-policy) loaded before the first tool call; no owning-skill procedure skipped.
- **R1 (RCA every anomaly):** every duplication verified against the CURRENT tree before editing (grep evidence in the summary); the stale `refs.go` pointer was R1'd as a claim to sweep, not copied forward.
- **R2 (no pre-existing/out-of-scope split):** all three items are the assignment's F4 tasks; only this PR's four files changed.
- **R3 (no duplication):** the two local copies are deleted; one canonical implementation each (kit.ValidatePlanSteps, spec.ProviderClasses, spec.ParseRemoteRef).
- **R4 (no workarounds):** no re-implementation, no shadow copy; the deletes are the consolidation.
- **R5 (no transitional path):** the copies and the stale comment are deleted in the same commit; no dual path.
- **R6 (git safety):** `git status` checked before the destructive actions (the delete of `validatePlanStepsPure`, the `parsedRef`/`parseRemoteRef`/`splitRepoAndSubPath` mirror, and the stale `refs.go` comment): tree clean at the PR head `1da65db` (no staged/unstaged changes); branch-only (feat/f4-cross-repo), no force-push, no direct main push, no hook bypass.
- **R10 (fresh-build proof):** full `go test ./...` from the final committed tree (output above) PLUS the pre-merge disposable-bed run on the branch-built plugin-box (R10 section above) — the required bed is executed, not deferred; the released-pin re-run is the F-wave close step (scheduled, and run by the wave, not postponed).

## Change classification

- Change class: refactor (cross-cutting dedup F4.1/F4.2/F4.3; behavior-preserving)
- Verification gate: full `go test ./...` on the final committed tree + `charly check run check-commands-local` (calver 2026.251.1828) on the disposable bed with the branch-built plugin-box.
- Attribution tier: `analysed on a live system` — the full plugin-box test suite AND the disposable-bed run executed live on the changed tree (branch-built binary, branch plugin-box compiled in); the released-pin re-run at the F-wave close upgrades the wave's evidence to fully-tested.

*Assisted-by: pi anthropic/claude-sonnet-4-20250514 (analysed on a live system)*