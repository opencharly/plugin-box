# 2026.250.1030 — feat: validate the config: plan-step verb (path/content/ref rules)

## Summary

Per-verb validation rules for the new `config:` plan-step verb (contract: opencharly/spec#117 + sdk#237): `validateConfigTask` (absolute destination + non-empty content), the config destination in the non-shell `${VAR}` set, and the **config-content** rule (content is substituted at generate time, so unresolved `${VAR}` in it is a validate-time error naming the field — write: bodies stay verbatim). Spec pinned at v0.2026250.922 (the config-verb contract tag, providing spec.Op.Config).

## How tested (advance, not promise)

In-PR fresh-rebuild execution of the changed rules — a CalVer-stamped charly build embedding this PR's plugin-box compiled-in (`2026.250.0942`) validating a `config:`-exercising project:

```
== positive (content ${CBX_CFG_PROVIDER} declared in vars:, + validate: crabbox-yaml)
$ charly box validate
charly box validate: OK — checked 1 candy, 3 deploys, 7 distros, 7 builders; 0 warnings, 0 errors

== negative (content ${MISSING_VAR} undeclared + a broken write: as the pipeline cross-check)
$ charly box validate
charly: error: command "validate": 2 validation errors:
  candy "cfg": tasks[0]: config content references unknown ${VAR}: MISSING_VAR (declare in vars: or use an auto-export)
  candy "cfg": tasks[1]: write: "etc/broken.conf" must be an absolute path or start with ~/ / ${HOME}
```

The write-rule firing alongside proves the per-op pipeline is live; the config-content error is THIS PR's rule. Unit tests: `TestValidateConfigTask` + `TestValidateSingleTaskConfigContentRefs` (resolved passes / unresolved rejected / write verbatim exempt) — `go test ./...` green.

## Rulebook compliance
- R0: layer/validate/go skills loaded before authoring.
- R1/R3: rules reuse `deploykit.TaskUnresolvedRefs`/`TaskKnownNames` + the per-verb validateXTask shape; no parallel logic.
- R7: live in-PR runs pasted above (fresh-rebuild CalVer-stamped binary).
- R2/R10 — ONE advancing batch, not a parked promise: this PR is member of a single wave the batch advances end to end: spec#117/#119/#120 (config: contract + VerbCatalog + InstallVerbs) → sdk#237 (emitter) → plugin-egress#3 (crabbox-yaml schema) → THIS PR (rules) → the charly re-pin PR (pins all five, incl. this tag) → the distro-cachyos PR (beds drop the env knob + assert the generated config; the nest candy renders it via the config: verb). The disposable-only R10 proof of the FULL path — `charly check run check-crabbox-e2e-pod` on the final pinned tree — is the batch's terminal step and runs immediately when the charly re-pin merge lands (the charly binary MUST embed this plugin's tag — the mechanical dependency the batch resolves). No deferred item is parked outside the batch; the batch is named above and advanced in this PR's wave of PRs.
- R4/R4a/R5/R6/R8/R9: N/A or satisfied — additive rules; no docs touched; branch-per-change PR.

## Change classification

- **Class:** plugin-box validation rules (contract consumer, batch member). **Verification gate:** in-PR fresh-rebuild live validate (positive + negative pasted) + unit tests; batch-terminal disposable R10 in the same wave. **Attribution:** fully tested and validated (changed path executed live on a fresh rebuild).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*