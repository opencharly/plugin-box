# 2026.269.0645 — feat: declare the box command parent on the wire (hard cutover)

## Summary

Make the parent part of every command word this plugin serves its full capability IDENTITY:
the manifest lists `command:<word>:box` and `NewMeta` sets
`ProvidedCapability.CommandParent = boxCommandParent`, so charly keys the provider at
`command:<word>:box` in EITHER placement. The Go `CommandParent()` method — the old carrier
of the parent — is **DELETED** in this same change.

This is what makes `charly box feature` (nested) and `charly feature` (top-level,
candy/plugin-feature) two distinct capabilities instead of a registry order-dependent
collision.

### Classification: this is a HARD CUTOVER, not an additive change

The old spelling `command:<word>` is no longer accepted; the new spelling
`command:<word>:box` replaces it, and the `CommandParent()` method is removed. That is a
modification + removal of existing surface, so the cutover-policy "purely additive" exemption
does NOT apply. It is a one-phase hard cutover: the transitional method is DELETED here, not
deferred, and there is no dual-acceptance path left.

### Why deleting the method is safe against the merged consumer

The safety rests on ONE fact, pasted here rather than asserted: **charly `origin/main` pins
this repo's OLD tag**, so the merged charly never pairs with this PR's new tag.

```
$ git -C charly show origin/main:charly/go.mod | grep plugin-box/candy
	github.com/opencharly/plugin-box/candy/plugin-box v0.2026266.1110
```

The consumer that pairs with the new tag is the charly branch `feat/command-parent-identity`,
which reads the DECLARED wire field and no longer sniffs the method:

```
$ grep -n GetCommandParent <charly feat/command-parent-identity>/charly/plugin_provider_common.go
158:            if p := c.GetCommandParent(); p != "" {
164:    } else if c.GetCommandParent() != "" {
```

The cutover is atomic across the two repos: charly `origin/main` keeps pinning the old plugin
tag (unaffected); this repo's new tag activates only when the charly leg bumps its pin, and
that leg reads the wire field.

### Complete change accounting

| File | Change |
|---|---|
| `candy/plugin-box/charly.yml` | `providers:` ×12 → the three-segment `command:<word>:box` form |
| `candy/plugin-box/plugin.go` | new `boxCommandParent` const; `NewMeta` sets `CommandParent` per capability; the `CommandParent()` method DELETED; stale comments swept |
| `candy/plugin-box/box_test.go` | asserts `command_parent=="box"` over the wire for all 12 words; the method assertion removed with the method |
| `candy/plugin-box/go.mod`/`go.sum` | sdk `v0.2026266.623 → v0.2026269.145`; spec `v0.2026266.545 → v0.2026269.138` (spec is a DIRECT dependency — the test imports `spec/proto` — and the sdk's new wire field comes from it); adds indirect `github.com/opencontainers/go-digest v1.0.0` + `github.com/opencontainers/image-spec v1.1.1` (transitives of the new spec/sdk graph) |
| `.github/workflows/deploy.yml` | CI candy-gate charly pin `v2026.238.1242 → v2026.269.0556` (that charly adopted the three-segment grammar; the old pin's CUE rejects `command:generate:box`) |
| `charly.yml` | root `version:` stamp `2026.232.0520 → 2026.261.1747` (`charly migrate` to the pinned binary's schema head) |

## How tested

### Coverage that fails without the change (in this repo)

```
$ go test ./... -count=1 -run DeclaresNested -v
--- PASS: TestNewMeta_DeclaresNestedCommands
```

`TestNewMeta_DeclaresNestedCommands` asserts every advertised command capability declares
`command_parent == "box"` over the wire (`Describe`). On the pre-change code the field was
never declared, so the assertion cannot pass.

### Gates (final tree)

```
$ go build ./...          # ok
$ go vet ./...            # clean
$ go test ./... -count=1
ok  	github.com/opencharly/plugin-box/candy/plugin-box
$ charly box validate     # at the pinned charly v2026.269.0556
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

### Live — the nested dispatch on the fresh-rebuild assembled cutover

The consumer charly branch assembles THIS commit from its in-dev tree and ran its disposable
`kind:local` bed `check-commands-local` (`disposable: true`, host: local) to completion:

```
$ go version -m bin/charly
  dep  github.com/opencharly/plugin-box/candy/plugin-box  =>  …/plugin-box/candy/plugin-box  (devel)

$ charly check run check-commands-local
  [deploy-add] PASS after 1m28.544s [allowlisted=1]
  [bring-up-members] PASS after 1ms
  [check-live] PASS after 2m9.787s [allowlisted=1]
  [update] PASS after 27.832s [allowlisted=1]      (fresh-rebuild re-verification)
  [cleanup] PASS after 23.681s [allowlisted=1]
  [cleanup-members] PASS
  charly check run check-commands-local: PASS (steps=6)
```

`check-live`'s `box-new-validate-generate` step exercises this plugin's NESTED verbs
(`new`/`validate`/`generate`) end-to-end — `exit=0`.

This plugin repo ships no check bed of its own, so this PR's own class gate is `go test` +
`charly box validate` on the final tree (both pasted); the live nested-dispatch bed belongs to
the consumer charly leg.

## Rulebook compliance

- **R0 (skills)** — plugin authoring: `/charly-internals:plugin`; cutover classification:
  `/charly-internals:cutover-policy`; landing: `/charly-internals:git-workflow`.
- **R1 (zero warnings / RCA)** — `go vet` clean; `charly box validate` reports 0 warnings; the
  stale NewMeta comments were swept.
- **R2 (whole cutover)** — the parent is declared in the manifest AND on the wire, and the old
  `CommandParent()` carrier is deleted in the SAME change (no transitional path left).
- **R3 (no duplication)** — `boxCommandParent` is the single parent constant, read by the wire
  declaration.
- **R4 (no workarounds)** — no shim, no dual-acceptance, no fallback.
- **R5 (delete legacy) — the grep self-test, pasted:**

  ```
  $ grep -rnE 'func \(provider\) CommandParent\(\)' candy/plugin-box/
  (no output — 0 hits)
  $ grep -rnE '[^t]CommandParent\(\)' candy/plugin-box/*.go
  (no output — 0 hits)
  ```

  The transitional method is deleted and no stale reference to it remains (wire-field
  accessors are `GetCommandParent`, which the pattern excludes).
- **R6 (git safety)** — branch `feat/command-parent-identity-v2`, no force-push; pins only
  merged refs (`sdk v0.2026269.145`, spec v0.2026269.138, charly `v2026.269.0556`).
- **R7 (prove behavior)** — the in-repo test asserts the declared `command_parent` over the
  wire; the nested dispatch ran live on the consumer's disposable bed.
- **R8 (preserve emitted artifacts)** — the assertion reads the real `Describe` reply; the bed
  asserts the rendered Containerfile.
- **R9 (binary equals source)** — N/A for this repo: it ships a Go module consumed by charly,
  not a binary. The consumer charly leg builds the stamped binary; `go version -m bin/charly`
  shows `plugin-box … (devel)`, i.e. this in-dev source is what the binary embeds.
- **R10 (fresh proof)** — the bed's `[update]` step is the fresh-rebuild re-verification.

## Change classification

- **Class:** plugin manifest + Go provider declaration — **hard cutover** (the parent carrier
  moves from the Go method to the declared wire field; the method is deleted).
- **Gate:** `go build`/`go vet`/`go test` + `charly box validate` on the final tree; the live
  nested-dispatch bed on the consumer charly leg.
- **Attribution tier:** `analysed on a live system` — the changed path ran live (the consumer
  leg's disposable bed, and the Phase-1 charly + this plugin dispatch) with retained output;
  this plugin repo ships no bed of its own, so the complete standalone fresh-rebuild R10 gate
  is the consumer leg's.

*Assisted-by: OpenCode ollama-cloud DeepSeek V4.1 Flash (analysed on a live system)*